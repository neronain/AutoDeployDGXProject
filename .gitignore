__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.coverage
bundles/
.DS_Store

# ประทับตอน install.sh — เป็นของแต่ละเครื่อง ไม่ใช่ของ repo
src/lmds/_build.py

# สถานะ editor ของเครื่องตัวเอง ไม่ใช่ของโปรเจกต์ — และรีโปนี้เป็นรีโปสาธารณะ
.obsidian/

# กันไฟล์ลับ · รีโปนี้ไม่เคยมีกฎ .env เลย ทั้งที่ push ขึ้นที่สาธารณะ
.env
.env.*
!.env.example
*.pem
*.key

# แผนอัพเกรดภายใน — มีรายละเอียดช่องโหว่ที่ยังไม่แก้และกลยุทธ์เชิงพาณิชย์
# ห้ามขึ้นรีโปสาธารณะจนกว่าจะตัดสินใจเรื่องการเผยแพร่
docs/UPGRADE-2026-09.md

# สถานะฟลีตจริง — ชื่อเครื่อง · IP · login ของไซต์ลูกค้า
# RELEASE.md §7 ชี้มาที่ไฟล์นี้แทนที่จะเขียนของจริงลงไปเอง
docs/FLEET-STATE.md
